# apiVersion: 1

#   - name: 'default'
#     org_id: 1
#     folder: ''
#     type: 'file'
#     options:
#       folder: '/var/lib/grafana/dashboards'
  # name of this dashboard configuration (not dashboard itself)
  # id of the org to hold the dashboard
  # name of the folder to put the dashboard (http://docs.grafana.org/v5.0/reference/dashboard_folders/)
  # type of dashboard description (json files)
  # where dashboards are